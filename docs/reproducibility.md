# Reproducibility

Experiments should include dataset snapshots, dependency versions, random seeds when relevant, and clear commands for rerunning analysis. Results should be treated as research outputs, not forecasts with guaranteed accuracy.
